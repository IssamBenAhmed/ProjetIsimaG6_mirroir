# Documentation Doxygen

Prérequis:
- `doxygen` installé (ex: `sudo apt install doxygen`)

Générer la documentation:

```bash
doxygen Doxyfile
```

La sortie HTML sera dans `docs/doxygen/html`.
Ouvrez `docs/doxygen/html/index.html` dans un navigateur pour visualiser.

